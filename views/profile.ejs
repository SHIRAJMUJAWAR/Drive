<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Profile</title>
</head>

<body class="min-h-screen bg-gradient-to-br from-[#020617] via-[#0f172a] to-[#020617]
             flex items-center justify-center text-white">

  <!-- Profile Card -->
  <div class="relative w-full max-w-lg p-10 rounded-3xl
              bg-white/5 backdrop-blur-xl
              border border-white/10
              shadow-[0_0_60px_rgba(16,185,129,0.25)]
              hover:shadow-[0_0_80px_rgba(16,185,129,0.45)]
              transition-all duration-500">

    <!-- Glow -->
    <div class="absolute -top-12 left-1/2 -translate-x-1/2
                w-36 h-36 rounded-full bg-emerald-500/30 blur-3xl"></div>

    <!-- Avatar -->
    <div class="flex justify-center mb-6">
      <div class="relative">
        <img src="/logo.png"
             class="w-28 h-28 rounded-full border-4 border-emerald-400
                    shadow-[0_0_30px_rgba(16,185,129,0.9)] object-cover" />
        <span class="absolute bottom-1 right-1 w-5 h-5 bg-green-400
                     rounded-full border-2 border-black"></span>
      </div>
    </div>

    <!-- Name -->
    <h1 class="text-3xl font-extrabold text-center tracking-wide">
       <%= user.username %>
    </h1>
    <p class="text-center text-gray-400 mb-8">
     • Cloud Drive
    </p>

    <!-- Info -->
    <div class="space-y-4">
      <div class="flex justify-between bg-white/5 p-4 rounded-xl border border-white/10">
        <span class="text-gray-400">Email</span>
        <span class="font-medium"><%= user.email %></span>
      </div>

      <div class="flex justify-between bg-white/5 p-4 rounded-xl border border-white/10">
        <span class="text-gray-400">Username</span>
        <span class="font-medium">@<%= user.username %></span>
      </div>

      <div class="flex justify-between bg-white/5 p-4 rounded-xl border border-white/10">
        <span class="text-gray-400">Uploaded Files</span>
        <span class="font-medium text-emerald-400"><%= user.length %></span>
      </div>
    </div>

    <!-- Actions -->
    <div class="mt-8 flex gap-4">
    <button
  class="flex-1 py-3 rounded-2xl font-semibold
         bg-gradient-to-r from-rose-500 via-red-500 to-red-600
         shadow-[0_0_30px_rgba(239,68,68,0.7)]
         hover:shadow-[0_0_55px_rgba(239,68,68,1)]
         hover:scale-[1.03]
         active:scale-[0.97]
         transition-all duration-300"> <a href="/"> Logout</a>
 
</button>

    </div>

  </div>
<!-- Go Back Button -->
<button onclick="history.back()"
  class="fixed top-6 left-6 z-50
         flex items-center gap-2 px-4 py-2.5
         rounded-xl bg-white/10 backdrop-blur-md
         border border-white/15
         text-white font-medium
         hover:bg-white/20
         hover:shadow-[0_0_25px_rgba(16,185,129,0.6)]
         transition-all duration-300">

  <!-- Arrow Icon -->
  <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5"
       fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
    <path stroke-linecap="round" stroke-linejoin="round"
          d="M15 19l-7-7 7-7" />
  </svg>

  Back
</button>

</body>
</html>
