<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Shiraj's Drive – Home</title>

  <style>
    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: Inter, system-ui, sans-serif;
      background: radial-gradient(circle at top, #020617, #000);
      color: #e5e7eb;
    }

    /* Header */
    header {
      position: fixed;
      top: 0;
      width: 100%;
      height: 70px;
      padding: 0 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: rgba(0,0,0,0.6);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid rgba(255,255,255,0.08);
      z-index: 50;
    }

    header h1 {
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 22px;
      font-weight: 800;
    }

    header img {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      box-shadow: 0 0 25px rgba(34,255,136,0.6);
    }

    header button {
      padding: 10px 18px;
      border-radius: 14px;
      border: none;
      cursor: pointer;
      font-weight: 700;
      color: black;
      background: linear-gradient(to right, #22ff88, #16a34a);
      box-shadow: 0 0 30px rgba(34,255,136,0.6);
      transition: all .3s ease;
    }

    header button:hover {
      transform: translateY(-1px);
      box-shadow: 0 0 45px rgba(34,255,136,0.9);
    }

    /* Main */
    main {
      padding-top: 110px;
      max-width: 1100px;
      margin: auto;
      padding-left: 24px;
      padding-right: 24px;
    }

    .hero {
      text-align: center;
      margin-bottom: 60px;
    }

    .hero h2 {
      font-size: 38px;
      font-weight: 900;
      margin-bottom: 12px;
    }

    .hero p {
      font-size: 16px;
      color: #9ca3af;
      max-width: 520px;
      margin: auto;
    }

    /* Cards */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
    }

    .card {
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 20px;
      padding: 24px;
      backdrop-filter: blur(16px);
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.04);
      transition: all .3s ease;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 0 40px rgba(34,255,136,0.2);
    }

    .card h3 {
      font-size: 20px;
      margin-bottom: 8px;
    }

    .card p {
      font-size: 14px;
      color: #9ca3af;
    }

    /* Upload Section */
    .upload {
      margin-top: 60px;
      text-align: center;
      padding: 40px;
      border-radius: 28px;
      background: linear-gradient(
        to bottom right,
        rgba(34,255,136,0.15),
        rgba(34,255,136,0.02)
      );
      border: 1px solid rgba(34,255,136,0.3);
      box-shadow: 0 0 60px rgba(34,255,136,0.35);
    }

    .upload h3 {
      font-size: 26px;
      font-weight: 800;
      margin-bottom: 8px;
    }

    .upload p {
      font-size: 14px;
      color: #bbf7d0;
      margin-bottom: 22px;
    }

    .upload button {
      padding: 14px 28px;
      border-radius: 18px;
      font-size: 16px;
      font-weight: 800;
      border: none;
      cursor: pointer;
      color: black;
      background: linear-gradient(to right, #22ff88, #4ade80);
      box-shadow: 0 0 40px rgba(34,255,136,0.8);
      transition: all .3s ease;
    }

    .upload button:hover {
      transform: scale(1.05);
      box-shadow: 0 0 60px rgba(34,255,136,1);
    }

  </style>
</head>

<body>

<header>
  <h1>
    <img src="/logo.png" alt="logo">
    Shiraj’s Drive
  </h1>

  <button onclick="upload()">Upload</button>
</header>

<main>

  <!-- Hero -->
  <section class="hero">
    <h2>Your files. Safe. Fast. Everywhere.</h2>
    <p>
      Store your images, videos, documents and backups securely in one place.
      Access them anytime, from any device.
    </p>
  </section>

  <!-- Info Cards -->
  <section class="grid">
    <div class="card">
      <h3>📁 Store Anything</h3>
      <p>Upload images, videos, PDFs, ZIPs and more without limits.</p>
    </div>

    <div class="card">
      <h3>⚡ Fast Access</h3>
      <p>Instant preview and quick downloads whenever you need.</p>
    </div>

    <div class="card">
      <h3>🔒 Secure Storage</h3>
      <p>Your files are protected with modern authentication.</p>
    </div>

    <div class="card">
      <h3>☁️ Cloud Sync</h3>
      <p>Access your drive from anywhere, anytime.</p>
    </div>
  </section>

  <!-- Upload CTA -->
  <section class="upload">
    <h3>Start uploading your files</h3>
    <p>No files yet? Upload your first file and build your drive.</p>
    <button onclick="upload()">Upload Your File</button>
  </section>

</main>

<script>
  function upload() {
    alert("Open upload modal / file picker here");
  }
</script>

</body>
</html>
